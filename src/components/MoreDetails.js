import React from 'react';

const MoreDetails = (props) => {
    console.log(props)
    return (
        <div>
            <h2>More Details</h2>
        </div>
    );
};

export default MoreDetails;